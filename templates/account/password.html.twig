{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - Dé-2-JP{% endblock %}

{% block content %}
    <div class="container mt-5">
        <h1 class="text-black">Modifier mon mot de passe</h1>
        - <a href="{{ path('account') }}">Retourner à mon compte</a>
        <hr>
        {% if notification %}
            <div class="alert alert-info">{{ notification }}</div>
        {% endif %}
        {{ form(form) }}
        <hr>
    </div>

{% endblock %}
