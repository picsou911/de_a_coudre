{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}Dé-2-JP{% endblock %}

{% block content %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-4">
            <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-thumbnail">
        </div>
        <div class="col-md-8">
            <h3><a href="{{ product.subtitle }}" target="_blank">{{ product.name }}</a></h3>
            <hr>
            <p>
                {{ product.description }}
            </p>
        </div>
    </div>
</div>
{% endblock %}
